# ===================================================================
# MODULE 9 — ANALYTICS & SYSTEM GOVERNANCE
# Copy to .env at repo root for docker-compose / local tooling.
# Backend reads these via dotenv — do not paste real secrets into source code.
# ===================================================================

# PostgreSQL (Compose service name when using docker-compose: DB_HOST=db)
DB_HOST=db
DB_PORT=5432
DB_USER=m9admin
DB_PASSWORD=m9secret
DB_NAME=nfasvp

# Backend
PORT=4000
NODE_ENV=development

# Frontend (Vite — baked at dev server start / build time)
VITE_API_URL=http://localhost:4000

# Optional: CORS
CORS_ORIGIN=http://localhost:3000
